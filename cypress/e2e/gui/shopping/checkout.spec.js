describe('Checkout Case', () => {
    beforeEach(() => {
        cy.api_login();
    });

    context('Positive Scenarios', () => {
        it('Checkout success', () => {
            
        });
    });
});